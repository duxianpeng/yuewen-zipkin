spring:
  application:
    name: yuewen-zipkin
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://${HOST_NAME:localhost}:1111/yuewen-discover/eureka/
